# Terraform — never commit state (contains DB password, keys) or local vars
terraform/.terraform/
terraform/*.tfstate
terraform/*.tfstate.*
terraform/*.tfplan
terraform/terraform.tfvars
terraform/crash.log

# The provider lock file SHOULD be committed — do not ignore .terraform.lock.hcl
